# SpaceMouse Compact 入力校正

対象機種: 3Dconnexion SpaceMouse Compact (`VID 256F`, `PID C635`)

## 2026-08-11 実機測定

各操作を通常使用時の強さで4〜5往復し、6軸の最小値と最大値を記録した。

| 意図した操作 | 主軸範囲 | 大きかった他軸成分 |
|---|---:|---|
| 左右平行移動 | `tx -350..+350` | `ty -110`, `rz +93` |
| 前後平行移動 | `ty -350..+350` | `tz +220`, `ry -122..+142`, `tx +133` |
| 押す／引く | `tz -350..+350` | `rz +125`, `ry +109` |
| 前後傾き | `rx -350..+350` | `ty -86`, `ry -68` |
| 左右傾き | `ry -350..+350` | `tx -81`, `tz +75` |
| 左右ひねり | `rz -350..+350` | `ty -132`, `tz +128`, `rx -102` |

前後平行移動ではZ軸へ最大220混入した。単一軸の閾値だけでは誤判定するため、
最大成分を優先する判定と入力ロックを採用する。

## 採用値

| 設定 | 値 | 目的 |
|---|---:|---|
| DOWN閾値 | `180` | 通常操作の約半分で確実に反応 |
| UP閾値 | `90` | 中央付近まで戻ってから解除 |
| 優先軸比率 | `1.25` | 最大軸が次点軸より明確な場合だけ採用 |
| 入力継続時間 | `40ms` | 瞬間的なノイズや横切りを無視 |
| 同時軸入力 | `1軸のみ` | Codex操作の意図しない多重発火を防止 |
| 入力ロック | `中央復帰まで` | 操作中に別軸へ切り替わるのを防止 |

ボタン入力には軸閾値を適用せず、HIDの押下・解放をそのまま使う。

## 再調整の基準

Codex接続後の実操作で問題が確認された場合のみ変更する。

- 重すぎる: DOWN閾値を `160` まで下げる
- 軽すぎる: DOWN閾値を `200` まで上げる
- 斜め操作で誤判定する: 優先軸比率を `1.35` へ上げる
- 反応が遅い: 継続時間を `30ms` へ下げる
- 瞬間的な誤発火がある: 継続時間を `50ms` へ上げる

一度に複数の値を変更せず、変更前後で同じ操作を測定する。
